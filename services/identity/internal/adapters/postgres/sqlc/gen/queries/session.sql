-- Code generated by sqlc. DO NOT EDIT.

-- name: CreateSession :one
INSERT INTO session (
    id,
    user_id,
    token_hash,
    created_at
) VALUES (
    $1,
    $2,
    $3,
    $4
) RETURNING
    id,
    user_id,
    token_hash,
    created_at
;

-- name: GetSessionByID :one
SELECT
    id,
    user_id,
    token_hash,
    created_at
FROM session
WHERE id = $1;

-- name: ListSessions :many
SELECT
    id,
    user_id,
    token_hash,
    created_at
FROM session
ORDER BY created_at DESC
LIMIT $1 OFFSET $2;

-- name: CountSessions :one
SELECT COUNT(*) AS total
FROM session
;

-- name: ListSessionsByUserID :many
SELECT
    id,
    user_id,
    token_hash,
    created_at
FROM session
WHERE user_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3;

-- name: CountSessionsByUserID :one
SELECT COUNT(*) AS total
FROM session
WHERE user_id = $1
;

-- name: ListSessionsByUserIDAsc :many
SELECT
    id,
    user_id,
    token_hash,
    created_at
FROM session
WHERE user_id = $1
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: ListSessionsByUserIDArray :many
SELECT
    id,
    user_id,
    token_hash,
    created_at
FROM session
WHERE user_id = ANY($1::uuid[])
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: CountSessionsByUserIDArray :one
SELECT COUNT(*) AS total
FROM session
WHERE user_id = ANY($1::uuid[])
;

-- name: UpdateSession :one
UPDATE session
SET
    token_hash = $2
WHERE id = $1
RETURNING
    id,
    user_id,
    token_hash,
    created_at
;

-- name: DeleteSession :execrows
DELETE FROM session WHERE id = $1;
