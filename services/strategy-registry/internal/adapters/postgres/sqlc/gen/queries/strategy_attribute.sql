-- Code generated by sqlc. DO NOT EDIT.

-- name: CreateStrategyAttribute :one
INSERT INTO strategy_attribute (
    id,
    strategy_id,
    slug,
    name,
    description,
    created_at,
    updated_at
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7
) RETURNING
    id,
    strategy_id,
    slug,
    name,
    description,
    created_at,
    updated_at
;

-- name: GetStrategyAttributeByID :one
SELECT
    id,
    strategy_id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy_attribute
WHERE id = $1;

-- name: ListStrategyAttributes :many
SELECT
    id,
    strategy_id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy_attribute
ORDER BY created_at DESC
LIMIT $1 OFFSET $2;

-- name: CountStrategyAttributes :one
SELECT COUNT(*) AS total
FROM strategy_attribute
;

-- name: ListStrategyAttributesWithSearch :many
SELECT
    id,
    strategy_id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy_attribute
WHERE strategy_id = sqlc.arg(strategy_id)
  AND (
    sqlc.narg(search)::text = '' OR
    slug ILIKE '%' || sqlc.narg(search)::text || '%'
    OR
    name ILIKE '%' || sqlc.narg(search)::text || '%'
    OR
    description ILIKE '%' || sqlc.narg(search)::text || '%'
)
ORDER BY
    CASE WHEN sqlc.arg(sort_asc)::bool THEN created_at END ASC,
    CASE WHEN NOT sqlc.arg(sort_asc)::bool THEN created_at END DESC
LIMIT sqlc.arg(page_limit) OFFSET sqlc.arg(page_offset);

-- name: CountStrategyAttributesWithSearch :one
SELECT COUNT(*) AS total
FROM strategy_attribute
WHERE strategy_id = sqlc.arg(strategy_id)
  AND (
    sqlc.narg(search)::text = '' OR
    slug ILIKE '%' || sqlc.narg(search)::text || '%' OR
    name ILIKE '%' || sqlc.narg(search)::text || '%' OR
    description ILIKE '%' || sqlc.narg(search)::text || '%' OR
    FALSE
)
;

-- name: GetStrategyAttributeBySlug :one
SELECT
    id,
    strategy_id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy_attribute
WHERE strategy_id = $1
  AND slug = $2
;

-- name: GetStrategyAttributeByName :one
SELECT
    id,
    strategy_id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy_attribute
WHERE strategy_id = $1
  AND name = $2
;

-- name: ListStrategyAttributesByStrategyID :many
SELECT
    id,
    strategy_id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy_attribute
WHERE strategy_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributesByStrategyID :one
SELECT COUNT(*) AS total
FROM strategy_attribute
WHERE strategy_id = $1
;

-- name: ListStrategyAttributesByStrategyIDAsc :many
SELECT
    id,
    strategy_id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy_attribute
WHERE strategy_id = $1
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: ListStrategyAttributesByStrategyIDArray :many
SELECT
    id,
    strategy_id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy_attribute
WHERE strategy_id = ANY($1::uuid[])
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributesByStrategyIDArray :one
SELECT COUNT(*) AS total
FROM strategy_attribute
WHERE strategy_id = ANY($1::uuid[])
;

-- name: UpdateStrategyAttribute :one
UPDATE strategy_attribute
SET
    slug = $2,
    name = $3,
    description = $4,
    updated_at = $5
WHERE id = $1
RETURNING
    id,
    strategy_id,
    slug,
    name,
    description,
    created_at,
    updated_at
;

-- name: DeleteStrategyAttribute :execrows
DELETE FROM strategy_attribute WHERE id = $1;
