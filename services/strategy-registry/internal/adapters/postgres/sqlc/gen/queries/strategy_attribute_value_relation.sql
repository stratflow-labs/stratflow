-- Code generated by sqlc. DO NOT EDIT.

-- name: CreateStrategyAttributeValueRelation :one
INSERT INTO strategy_attribute_value_relation (
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7
) RETURNING
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
;

-- name: GetStrategyAttributeValueRelationByID :one
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE id = $1;

-- name: ListStrategyAttributeValueRelations :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
ORDER BY created_at DESC
LIMIT $1 OFFSET $2;

-- name: CountStrategyAttributeValueRelations :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value_relation
;

-- name: ListStrategyAttributeValueRelationsByFromAttributeID :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE from_attribute_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributeValueRelationsByFromAttributeID :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value_relation
WHERE from_attribute_id = $1
;

-- name: ListStrategyAttributeValueRelationsByFromAttributeIDAsc :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE from_attribute_id = $1
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: ListStrategyAttributeValueRelationsByFromAttributeIDArray :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE from_attribute_id = ANY($1::uuid[])
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributeValueRelationsByFromAttributeIDArray :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value_relation
WHERE from_attribute_id = ANY($1::uuid[])
;

-- name: ListStrategyAttributeValueRelationsByFromValueID :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE from_value_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributeValueRelationsByFromValueID :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value_relation
WHERE from_value_id = $1
;

-- name: ListStrategyAttributeValueRelationsByFromValueIDAsc :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE from_value_id = $1
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: ListStrategyAttributeValueRelationsByFromValueIDArray :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE from_value_id = ANY($1::uuid[])
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributeValueRelationsByFromValueIDArray :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value_relation
WHERE from_value_id = ANY($1::uuid[])
;

-- name: ListStrategyAttributeValueRelationsByToAttributeID :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE to_attribute_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributeValueRelationsByToAttributeID :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value_relation
WHERE to_attribute_id = $1
;

-- name: ListStrategyAttributeValueRelationsByToAttributeIDAsc :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE to_attribute_id = $1
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: ListStrategyAttributeValueRelationsByToAttributeIDArray :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE to_attribute_id = ANY($1::uuid[])
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributeValueRelationsByToAttributeIDArray :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value_relation
WHERE to_attribute_id = ANY($1::uuid[])
;

-- name: ListStrategyAttributeValueRelationsByToValueID :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE to_value_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributeValueRelationsByToValueID :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value_relation
WHERE to_value_id = $1
;

-- name: ListStrategyAttributeValueRelationsByToValueIDAsc :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE to_value_id = $1
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: ListStrategyAttributeValueRelationsByToValueIDArray :many
SELECT
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
FROM strategy_attribute_value_relation
WHERE to_value_id = ANY($1::uuid[])
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributeValueRelationsByToValueIDArray :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value_relation
WHERE to_value_id = ANY($1::uuid[])
;

-- name: UpdateStrategyAttributeValueRelation :one
UPDATE strategy_attribute_value_relation
SET
    updated_at = $2
WHERE id = $1
RETURNING
    id,
    from_attribute_id,
    from_value_id,
    to_attribute_id,
    to_value_id,
    created_at,
    updated_at
;

-- name: DeleteStrategyAttributeValueRelation :execrows
DELETE FROM strategy_attribute_value_relation WHERE id = $1;
