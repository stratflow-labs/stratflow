-- Code generated by sqlc. DO NOT EDIT.

-- name: CreateStrategy :one
INSERT INTO strategy (
    id,
    slug,
    name,
    description,
    created_at,
    updated_at
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6
) RETURNING
    id,
    slug,
    name,
    description,
    created_at,
    updated_at
;

-- name: GetStrategyByID :one
SELECT
    id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy
WHERE id = $1;

-- name: ListStrategies :many
SELECT
    id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy
ORDER BY created_at DESC
LIMIT $1 OFFSET $2;

-- name: CountStrategies :one
SELECT COUNT(*) AS total
FROM strategy
;

-- name: ListStrategiesWithSearch :many
SELECT
    id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy
WHERE (
    sqlc.narg(search)::text = '' OR
    slug ILIKE '%' || sqlc.narg(search)::text || '%'
    OR
    name ILIKE '%' || sqlc.narg(search)::text || '%'
    OR
    description ILIKE '%' || sqlc.narg(search)::text || '%'
)
ORDER BY
    CASE WHEN sqlc.arg(sort_asc)::bool THEN created_at END ASC,
    CASE WHEN NOT sqlc.arg(sort_asc)::bool THEN created_at END DESC
LIMIT sqlc.arg(page_limit) OFFSET sqlc.arg(page_offset);

-- name: CountStrategiesWithSearch :one
SELECT COUNT(*) AS total
FROM strategy
WHERE (
    sqlc.narg(search)::text = '' OR
    slug ILIKE '%' || sqlc.narg(search)::text || '%' OR
    name ILIKE '%' || sqlc.narg(search)::text || '%' OR
    description ILIKE '%' || sqlc.narg(search)::text || '%' OR
    FALSE
)
;

-- name: GetStrategyBySlug :one
SELECT
    id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy
WHERE slug = $1
;

-- name: GetStrategyByName :one
SELECT
    id,
    slug,
    name,
    description,
    created_at,
    updated_at
FROM strategy
WHERE name = $1
;

-- name: UpdateStrategy :one
UPDATE strategy
SET
    slug = $2,
    name = $3,
    description = $4,
    updated_at = $5
WHERE id = $1
RETURNING
    id,
    slug,
    name,
    description,
    created_at,
    updated_at
;

-- name: DeleteStrategy :execrows
DELETE FROM strategy WHERE id = $1;
