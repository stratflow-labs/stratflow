-- Code generated by sqlc. DO NOT EDIT.

-- name: CreateStrategyAttributeValue :one
INSERT INTO strategy_attribute_value (
    id,
    strategy_attribute_id,
    slug,
    value,
    created_at,
    updated_at
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6
) RETURNING
    id,
    strategy_attribute_id,
    slug,
    value,
    created_at,
    updated_at
;

-- name: GetStrategyAttributeValueByID :one
SELECT
    id,
    strategy_attribute_id,
    slug,
    value,
    created_at,
    updated_at
FROM strategy_attribute_value
WHERE id = $1;

-- name: ListStrategyAttributeValues :many
SELECT
    id,
    strategy_attribute_id,
    slug,
    value,
    created_at,
    updated_at
FROM strategy_attribute_value
ORDER BY created_at DESC
LIMIT $1 OFFSET $2;

-- name: CountStrategyAttributeValues :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value
;

-- name: ListStrategyAttributeValuesWithSearch :many
SELECT
    id,
    strategy_attribute_id,
    slug,
    value,
    created_at,
    updated_at
FROM strategy_attribute_value
WHERE strategy_attribute_id = sqlc.arg(strategy_attribute_id)
  AND (
    sqlc.narg(search)::text = '' OR
    slug ILIKE '%' || sqlc.narg(search)::text || '%'
    OR
    value ILIKE '%' || sqlc.narg(search)::text || '%'
)
ORDER BY
    CASE WHEN sqlc.arg(sort_asc)::bool THEN created_at END ASC,
    CASE WHEN NOT sqlc.arg(sort_asc)::bool THEN created_at END DESC
LIMIT sqlc.arg(page_limit) OFFSET sqlc.arg(page_offset);

-- name: CountStrategyAttributeValuesWithSearch :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value
WHERE strategy_attribute_id = sqlc.arg(strategy_attribute_id)
  AND (
    sqlc.narg(search)::text = '' OR
    slug ILIKE '%' || sqlc.narg(search)::text || '%' OR
    value ILIKE '%' || sqlc.narg(search)::text || '%' OR
    FALSE
)
;

-- name: GetStrategyAttributeValueBySlug :one
SELECT
    id,
    strategy_attribute_id,
    slug,
    value,
    created_at,
    updated_at
FROM strategy_attribute_value
WHERE strategy_attribute_id = $1
  AND slug = $2
;

-- name: ListStrategyAttributeValuesByStrategyAttributeID :many
SELECT
    id,
    strategy_attribute_id,
    slug,
    value,
    created_at,
    updated_at
FROM strategy_attribute_value
WHERE strategy_attribute_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributeValuesByStrategyAttributeID :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value
WHERE strategy_attribute_id = $1
;

-- name: ListStrategyAttributeValuesByStrategyAttributeIDAsc :many
SELECT
    id,
    strategy_attribute_id,
    slug,
    value,
    created_at,
    updated_at
FROM strategy_attribute_value
WHERE strategy_attribute_id = $1
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: ListStrategyAttributeValuesByStrategyAttributeIDArray :many
SELECT
    id,
    strategy_attribute_id,
    slug,
    value,
    created_at,
    updated_at
FROM strategy_attribute_value
WHERE strategy_attribute_id = ANY($1::uuid[])
ORDER BY created_at ASC
LIMIT $2 OFFSET $3;

-- name: CountStrategyAttributeValuesByStrategyAttributeIDArray :one
SELECT COUNT(*) AS total
FROM strategy_attribute_value
WHERE strategy_attribute_id = ANY($1::uuid[])
;

-- name: UpdateStrategyAttributeValue :one
UPDATE strategy_attribute_value
SET
    slug = $2,
    value = $3,
    updated_at = $4
WHERE id = $1
RETURNING
    id,
    strategy_attribute_id,
    slug,
    value,
    created_at,
    updated_at
;

-- name: DeleteStrategyAttributeValue :execrows
DELETE FROM strategy_attribute_value WHERE id = $1;
